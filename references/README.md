# references/(v0.3 旧 · 已弃用)

> **状态:v0.3 旧运行层,不在 v0.4 加载路径。保留作历史参考。**

v0.4 运行层已迁移到 `shared/`(`core-principles.md` + `tool-permissions.md`)+ `skills/<task>/SKILL.md`。本目录的 7 个文件(七门详细判据、模板、协议、状态机等)是 **v0.3 单 skill 架构**的产物,v0.4 不引用、不加载。

- 运行层自检已确认:v0.4 活文件(skills / shared / scripts / .claude-plugin)**零引用**本目录。
- 真实执行暴露缺口时,可回看本目录补**最小规则**,但**不预告迁入、不双轨维护**。
- 若日后确认不再需要 v0.3 细节作参考,可整体删除或移到 `development/archives/`。
