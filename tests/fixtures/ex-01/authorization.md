# 访问范围(EX-01)

## 允许
- 读:`kb/`(本 fixture 知识库)
- 写:`out/`(产物)

## 禁止
- 访问 `expected/`(**对生产 Agent 不可见**)
- 访问 `prompts/`、`backend/`(本场景无)
- 访问本 fixture 之外的任何目录或外部仓库

## 外部仓库授权
- 未授权(四条件不齐)。如需外部信息 → 停止 + 生成可转交的信息请求,不访问。
