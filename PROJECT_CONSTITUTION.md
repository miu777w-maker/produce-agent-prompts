# Produce Agent Prompts · 项目最高指示(PROJECT_CONSTITUTION)

> 本文件是 `produce-agent-prompts` 仓库的**最高约束与最终目标定义**。
> 修改、生成、评审或扩展本仓库前**必须先完整读取本文件**。
> 任何 Skill、共享文件、测试、README、历史或既有实现与本文件冲突时,**以本文件为准**。
> development 工作区已清空;历史(process-history 00–43、旧 references)保留在 `archive/development-history` 分支,不构成当前需求,**不得自动恢复其中的旧方案**。

## 一、最终目标

本仓库用于项目知识库环境,目标是让 Agent 根据项目知识库稳定生产项目实际需要的:

1. 系统 Agent Prompts;
2. Agent Evals;
3. Agent Rubrics。

并在需要时支持:

4. Prompt Inspection;
5. Prompt Revision;
6. Runtime Integration Validation。

衡量仓库是否成功,只看以下结果:

- 产物范围是否正确;
- 产物内容是否正确;
- 是否忠实使用知识库;
- 是否避免越权获取知识;
- 是否能发现影响产物可用性的缺失、错误与冲突;
- Skill 是否足够简单,能够稳定执行。

**不以文件数量、流程完整度、状态数量、文档数量或架构复杂度作为质量标准。**

## 二、知识来源与环境边界

### 1. 知识库环境

Prompt、Eval 和 Rubric 默认只从当前项目知识库获取正式知识。

进入知识库的内容默认视为项目正式依据,除非存在明显错误、冲突、断链、版本问题或非最终标记。

Agent 应通过目录、索引、话题、链接和定向检索找到当前任务需要的内容,**不默认读取整个知识库**。

### 2. 外部仓库

知识库内运行的 Skill 不得主动访问后端、前端或其他项目仓库。

只有同时满足以下条件,才允许定向只读访问外部仓库:

1. 用户主动、明确要求;
2. 用户提供具体仓库地址;
3. 用户说明访问目的;
4. 用户限定访问范围。

缺少外部信息时,应列出缺失内容和影响,生成可由人员转交给其他项目 Agent 的信息请求,**不得自行越权获取**。

### 3. 知识库与代码的关系

- 知识库说明项目**预期**是什么;
- 代码、Schema 和日志说明**当前实际实现**是什么;
- 二者不一致时,必须区分 Prompt 误写、知识库过时、后端偏差或暂时无法判断;
- **不得静默选择任一方覆盖另一方**;
- 修改哪一方由项目人员决定。

## 三、共享的七类 Agent 事实

所有生产和校验任务都需要根据当前任务,明确以下七类 Agent 事实:

1. **目标与范围**——Agent 要完成什么、服务谁、任务边界、需要哪些产物或执行单元。
2. **运行环境**——Agent 有哪些工具,可能收到哪些系统 / 用户消息、状态和历史,输出由谁消费,异常如何处理。
3. **关键时机的上下文与概念绑定**——Agent 在具体执行时刻实际能看到什么;Prompt / Eval / Rubric 中引用的术语、代词、概念是否有明确输入来源。
4. **业务、角色与行为边界**——角色、目标、服务对象、语气、应做与不应做的行为。
5. **职责与信息可见性**——各执行单元分别负责什么、能看到什么;避免职责重复、信息泄漏和无关内容加载。
6. **关键约束与注意力安排**——哪些要求高风险、易漂移或必须重点遵循;它们在实际运行中是否有有效载体和时机。
7. **知识疑点、缺失与冲突**——是否存在错误、缺失、矛盾、版本问题、来源断裂或不可验证的假设。

**七类事实是共享事实基础,不代表所有 Skill 使用同一种生产流程。** 各 Skill 只读取与当前任务有关的事实,并按自己的职责转化为产物。

## 四、各 Skill 的独立职责

### 1. Prompt Creation
把知识库中的 Agent 事实转化为系统 Prompts。服从知识库规定的文件数量、名称、目录和拆分;把必要事实和约束放入正确的 Prompt;保证每个执行单元只看到必要信息;保留生产 / 校验阶段所需的元信息;关键资料缺失时停止并提出补充请求。**不负责**:生成 Eval / Rubric / 外部交接;擅自访问后端;无 KB 依据时编造运行时输入;用职责声明抵消 system 与 task 的冲突。

### 2. Agent Eval Creation
把知识库中的 Agent 事实转化为评测场景。明确被测 Agent 和行为范围;设计正常 / 边界 / 异常 / 冲突 / 缺失 / 工具 / 组合等必要场景;场景数量与结构来源于业务风险而非 Prompt 文件数;需要评分细则时调用 Agent Rubric Creation。**不负责**:把 Prompt 当正确答案;因现有 Prompt 的能力或错误降低评测要求;自行代替 Rubric 设计完整评分细则;修改 Prompt。

### 3. Agent Rubric Creation
把知识库中的 Agent 事实转化为评价完整 Agent 表现的评分细则。Rubric 评价的是 Agent 的**可观察行为与结果**,不是 Prompt 文本写作质量。按任务选择必要评分维度;生成可观察、可验证、可打分、尽量正交的条目;明确评分标度、通过 / 失败条件或分数档位。**不负责**:生成 Eval 场景 / 用户输入 / 测试数据 / 执行方法;把七类事实机械转换成七条 Rubric;把 Prompt 当知识来源或正确答案;据 Prompt 文件数决定 Rubric 数量;修改 Prompt 或 Eval。

### 4. Prompt Inspection
根据知识库和七类 Agent 事实检查已有 Prompt,**只报告问题,不修改 Prompt**。未建立正式流程前必须明确返回"当前未实现",**不得使用通用能力伪装完成**。

### 5. Prompt Revision
根据已确认的问题和知识库依据,在用户授权范围内修改 Prompt。**不得自动修改 Eval 或 Rubric**;如修改可能影响它们,只记录重新评估需求并交回对应 Skill。未建立正式流程前必须明确返回"当前未实现"。

### 6. Runtime Integration Validation
在用户明确授权后,把知识库与 Prompt 中的预期契约同后端实际实现进行**只读对照**。检查:Prompt 文件与配置对应、消息装配顺序、每个 Agent 实收信息、动态上下文注入、工具 Schema / 参数 / 枚举 / 返回、输出消费与异常路径、system / task / reminder / anchor 跨层冲突。**只生成差异、归因和上线阻断报告**,不修改 Prompt / KB / 代码,不重新决定业务角色、语气和产品策略。

## 五、Prompt、Eval 与 Rubric 的关系

Prompt、Eval 和 Rubric 可以在同一项目阶段同步形成,但**生产流程相互独立**。

- Prompt 规定 Agent 应如何工作;
- Eval 提供测试 Agent 的场景;
- Rubric 规定如何评价 Agent 的实际表现;
- 三者都以知识库为正式事实依据,**互不充当正确答案**;
- Prompt 文件数不决定 Eval 场景数或 Rubric 条目数;
- Eval 需要评分细则时可调用 Rubric Skill;Rubric 也可被用户独立调用;
- 是否合并文件、如何命名和放置,以知识库规范为准。

## 六、关键资料缺失时的处理

以下信息缺失且会改变产物时,**必须停止相关生成,不得自行假设**:

- 目标 Agent 或被评对象;
- 产物范围;
- 正式文件名称或拆分规则;
- 运行时输入、工具或消息;
- 关键概念的输入来源;
- 角色和职责边界;
- 关键业务规则;
- Rubric 的评价对象、评分用途或评分标度;
- 影响正确性的知识冲突。

停止时只需输出:① 缺失内容;② 它影响哪些产物或结论;③ 为什么无法安全继续;④ 需要人员补充什么 / 向哪个责任方询问什么。

非关键细节缺失可标记并继续,**但不得把推测写成事实**。

## 七、渐进式披露与最小运行上下文

每次任务只加载:

1. 当前任务 Skill;
2. 当前任务需要的共享事实定义;
3. 当前任务权限;
4. 当前阶段必要的知识库内容。

**不得**因为仓库中存在其他 Skill、tests、历史或旧 references,就自动读取它们。

- Prompt Creation 不加载 Eval、Rubric 或 Runtime 流程;
- Eval Creation 不加载 Prompt 写作流程;
- Rubric Creation 不加载 Eval 场景生成流程;
- Runtime Validation 不重做 Prompt、Eval 或 Rubric 的业务设计;
- 历史与 tests 默认不进入生产任务上下文。

## 八、简化原则

新增任何规则、文件、状态、模板、报告或流程前,必须回答:

> 如果不增加它,是否会直接导致**产物范围错误、内容错误、越权访问,或无法发现重要上线风险**?

答案为 否,就不应加入运行层。

其他约束:

- 不为尚未发生的问题预建复杂状态机;
- 不要求每项事实生成独立过程文件;
- 不把开发历史变成当前执行规则;
- 不为结构完整而创建占位能力;
- 不以更多文件和更多检查作为质量证明;
- 真实使用暴露问题后,再做最小修正。

## 九、变更纪律

修改仓库时:

1. 先确认本次只影响哪个 Skill;
2. 只读取与该修改直接相关的运行文件;
3. 不根据历史自动扩大范围;
4. 修改后做最小静态检查和针对性行为测试;
5. 没有真实证据时,不宣称能力成熟或测试通过;
6. 不因修改一个 Skill 自动改写其他产物;
7. 与本文件冲突的旧规则应删除或降为历史,**不得并存**。

## 十、目标与优先级

本仓库各能力的优先顺序与目标:

1. **Prompt Creation**——目标是能稳定用于真实知识库任务;
2. **Agent Rubric Creation**——目标是稳定生成可验证、可打分、正交的评分细则;
3. **Agent Eval Creation**——目标是独立生成评测场景,并按需调用 Rubric;
4. **Runtime Integration Validation**——目标是完成受控只读验证;
5. **Inspection 与 Revision**——等真实需要时再建立最小流程。

> 这是**目标与顺序**,不是当前成熟度宣称(当前成熟度见 `VERSION.md`)。仓库 Agent 不得因追求"完整体系"改变这个优先级。
