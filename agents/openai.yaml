interface:
  display_name: "Agent Prompt Producer"
  short_description: "Build and validate knowledge-grounded Agent prompt systems"
  default_prompt: "Use $prompt-creation to derive and produce the prompts needed for this Agent system from its project knowledge base; use $agent-eval-creation for eval scenarios, $agent-rubric-creation for rubrics, and $runtime-integration-validation for backend runtime checks."
